# -*- coding: utf-8 -*-
# @File    : {{klass.name}}.py
# @AUTH    : model

import json
from document_utils.consts import undefined
from common.Decorator.render import render
from common.Utils.log_utils import getLogger
from common.Helpers.Helper_pagenate import Page
from result import SuccessData
from ...BaseConsts import *
from ...BaseViews import BaseHandler
from ..utils.{{klass.name}} import {{klass.name}}, {{klass.name | lower}}_schema

log = getLogger("views/{{klass.name | lower}}")


class {{klass.name}}Handler(BaseHandler):
    @render
    {{ 'async ' if aio }}def get(self, {{klass.name | lower}}_id=None):
        if {{klass.name | lower}}_id:
            {{klass.name | lower}} = {{ 'await ' if aio }}{{klass.name}}.select(id={{klass.name | lower}}_id)
            return SuccessData(
                data={{ 'await ' if aio }}{{klass.name | lower}}.to_front()
            )
        else:
            search_params = json.loads(self.get_argument("search", '{}'))
            order_by = self.get_argument("order_by", "")
            use_pager = int(self.get_argument("use_pager", 1))
            page = int(self.get_argument("page", 1))
            items_per_page = int(self.get_argument("items_per_page", 20))

            item_count = {{ 'await ' if aio }}{{klass.name}}.count(**search_params)
            if use_pager:
                search_params.update({
                    "limit": items_per_page,
                    "skip": (page - 1) * items_per_page
                })
            order_by = [o for o in order_by.split(";") if bool(o)]
            {{klass.name | lower}}_cursor = {{klass.name}}.search(**search_params).order_by(order_by)
            data = [{{ 'await ' if aio }} {{klass.name | lower}}.to_front() {{ 'async ' if aio }}for {{klass.name | lower}} in {{klass.name | lower}}_cursor]
            pager = Page(data, use_pager=use_pager, page=page, items_per_page=items_per_page, item_count=item_count)
            return SuccessData(
                data=pager.items, 
                info=pager.info
            )


    @render
    {{ 'async ' if aio }}def post(self, {{klass.name | lower}}_id=None):
        if {{klass.name | lower}}_id:
            params = {{klass.name | lower}}_schema.load(self.arguments, partial=True)
            old_{{klass.name | lower}} = {{ 'await ' if aio }}{{klass.name}}.select(id={{klass.name | lower}}_id)
            new_{{klass.name | lower}} = {{ 'await ' if aio }}{{klass.name | lower}}.copy(**params.data)
            return SuccessData(
                id=new_{{klass.name | lower}}.id
            )
        else:
            params = {{klass.name | lower}}_schema.load(self.arguments)
            {{klass.name | lower}} = {{ 'await ' if aio }}{{klass.name}}.create(**params.data)
            return SuccessData(
                id={{klass.name | lower}}.id
            )

    @render
    {{ 'async ' if aio }}def put(self, {{klass.name | lower}}_id=None):
        params = {{klass.name | lower}}_schema.load(self.arguments)
        {{klass.name | lower}} = {{ 'await ' if aio }}{{klass.name}}.find_and_update(id={{klass.name | lower}}_id, **params.data)
        return SuccessData(
            id={{klass.name | lower}}.id
        )

    @render
    {{ 'async ' if aio }}def patch(self, {{klass.name | lower}}_id=None):
        params = {{klass.name | lower}}_schema.load(self.arguments, partial=True)
        {{klass.name | lower}} = {{ 'await ' if aio }}{{klass.name}}.find_and_update(id={{klass.name | lower}}_id, **params.data)
        return SuccessData(
            id={{klass.name | lower}}.id
        )

    @render
    {{ 'async ' if aio }}def delete(self, {{klass.name | lower}}_id=None):
        count = {{ 'await ' if aio }}{{klass.name}}.find_and_delete(id={{klass.name | lower}}_id)
        return SuccessData(
            count=count
        )

    def set_default_headers(self):
        self._headers.add("version", "1")
